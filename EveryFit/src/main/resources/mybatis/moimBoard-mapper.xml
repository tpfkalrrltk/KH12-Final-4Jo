<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="MoimBoard">

	<insert id="add">
		insert into moim_board (
		moim_board_no,
		moim_no,
		member_email,
		moim_board_title,
		moim_board_content,
		moim_board_category )
		values(
		#{moimBoardNo},
		#{moimNo},
		#{memberEmail},
		#{moimBoardTitle},
		#{moimBoardContent},
		#{moimBoardCategory})
	</insert>

	<select id="sequence" resultType="int">
		select moim_board_seq.nextval
		from dual
	</select>

	<select id="list" resultType="MoimBoardDto">
		select * from moim_board order by
		moim_board_no asc
	</select>

	<update id="edit">
		update moim_board
		<set>
			<if test="moimBoardTitle != null">
				moim_board_title =
				#{moimBoardTitle},
			</if>
			<if test="moimBoardContent != null">
				moim_board_content =
				#{moimBoardContent},
			</if>
			<if test="moimBoardCategory != null">
				moim_board_category =
				#{moimBoardCategory},
			</if>

		</set>
		where moim_board_no = #{moimBoardNo}
	</update>

	<delete id="delete">
		delete moim_board where moim_board_no = #{moimBoardNo}
	</delete>

	<select id="selectOne" resultType="MoimBoardDto">
		select * from moim_board where
		moim_board_no = #{moimBoardNo}
	</select>
<!-- 모임 번호에 따른 모임 게시판 목록 -->
	<select id="listByMoimNo" resultType="MoimBoardDto">
		SELECT * FROM moim_board WHERE moim_no= #{moimNo}
	</select>

<!-- 모임번호와 카테고리에 따른 모임 게시판 목록 -->
	<select id="listByMoimCategory" resultType="MoimBoardDto">
		SELECT * FROM moim_board WHERE moim_no= #{moimNo} AND moim_board_no_category = #{moimBoardCategory};
	</select>

<!-- 모임번호와 카테고리에 따른 모임 게시판 목록2 -->	
	<select id="listByMoimNoSortedByCategory" resultType="MoimBoardDto">
    SELECT * FROM moim_board
    WHERE moim_no = #{moimNo}
      AND moim_board_category = #{category}
    ORDER BY moim_board_no DESC
</select>
</mapper>